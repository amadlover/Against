#pragma once

#include "phy_asset.h"
#include "error.h"

typedef struct _scene_physics_obj
{

} scene_physics_obj;

AGAINST_RESULT scene_physics_init (const char* file_path, scene_physics_obj* out_data);